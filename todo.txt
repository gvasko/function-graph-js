View:

app/
    List: add(), clean()
        ListItem: remove(), move()
            FunctionGraphConfig: enabled
                //FunctionType in {ax2bxc, ...}
                //FunctionParameters: a, b, c
                AbstractFunction 
                DisplayAttributes: color, pattern
    FunctionView
        Table
        Chart
        Grid
        
        
Data
    Document
        FunctionList
        ViewSettings: 
            function: enabled, color, line pattern
            table/grid: parameter-domain, step (several configurations stored beforehand)
            chart: viewport (several configurations stored beforehand)


+testing
